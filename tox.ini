[tox]
envlist = py37

[testenv]
deps =
    -r requirements.txt
    -r requirements-dev.txt
commands =
    black --check tfrecord_converter tests
    flake8 tfrecord_converter
    isort -rc -c tfrecord_converter tests
    pytest
